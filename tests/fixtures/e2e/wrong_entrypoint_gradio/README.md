# Wrong Entrypoint Gradio Demo

A Gradio demo that echoes back the input with the current trace ID.

**Important**: The real entry point is `gradio_app.py`, not `app.py`.

## Usage

```bash
python gradio_app.py
```
