# Conda PyTorch Demo

Tiny fixture for dry-run Conda/PyTorch environment planning.

It does not download a real model. The app exposes a standard-library HTTP trace echo endpoint so verify can prove request handling without GPU.
