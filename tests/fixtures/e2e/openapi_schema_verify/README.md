# OpenAPI Schema Verify Demo

A FastAPI demo that exposes POST endpoints via OpenAPI schema.

The verify should discover `/openapi.json` and use POST with trace ID body.

## Usage

```bash
python app.py
```
