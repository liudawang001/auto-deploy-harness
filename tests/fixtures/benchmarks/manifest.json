{
  "cases": [
    {
      "id": "model_download_resume",
      "purpose": "验证模型下载中断后可通过 .part 和 Range 续传",
      "expected_signal": "Range header is emitted when partial file exists"
    },
    {
      "id": "cache_hit",
      "purpose": "验证已完整下载的模型文件不会重复下载",
      "expected_signal": "file status becomes cached"
    },
    {
      "id": "streamlit_error_page",
      "purpose": "验证 Streamlit 页面虽然 HTTP 200 但包含错误标记时不能通过",
      "expected_signal": "streamlit_dom_probe status is fail"
    },
    {
      "id": "verify_false_positive_http_200",
      "purpose": "验证 HTTP 200 但没有当前 trace 时不能判定成功",
      "expected_signal": "verify status remains uncertain"
    },
    {
      "id": "gradio_config_discovery",
      "purpose": "验证 Gradio /config discovery 能构造正确 POST trace 请求",
      "expected_signal": "request is sent to /api/predict with fn_index"
    },
    {
      "id": "repair_policy_reject",
      "purpose": "验证权限不足时 repair action 会被 policy 拒绝",
      "expected_signal": "policy allowed is false"
    },
    {
      "id": "checksum_failure",
      "purpose": "验证模型文件 checksum 不一致时不会进入缓存成功态",
      "expected_signal": "download status becomes failed with sha256 mismatch"
    },
    {
      "id": "browser_dom_trace",
      "purpose": "验证浏览器 DOM 中出现当前 trace 时可以作为强证据",
      "expected_signal": "browser_dom_probe status is pass"
    },
    {
      "id": "parallel_model_download",
      "purpose": "验证模型下载器可以并发下载多个必要文件且保持 manifest 顺序",
      "expected_signal": "two files are downloaded with max_workers > 1"
    },
    {
      "id": "etag_cache_invalidation",
      "purpose": "验证远端 etag 变化时本地缓存不会被误当成有效",
      "expected_signal": "cached file is redownloaded when etag metadata mismatches"
    },
    {
      "id": "cache_cleanup_plan",
      "purpose": "验证模型缓存清理可以先 dry-run 生成候选，再受控删除",
      "expected_signal": "old cache entry is selected and deleted only when dry_run is false"
    }
  ]
}
