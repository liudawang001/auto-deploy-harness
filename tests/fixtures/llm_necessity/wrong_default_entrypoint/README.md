# Wrong Default Entrypoint Demo

This is a Gradio demo where the main app is in `gradio_app.py`, not `app.py`.

## Running

```bash
pip install -r requirements.txt
python gradio_app.py
```

The app will start on port 7860.
