# Dependency Conflict Pydantic Demo

This demo uses an old version of Gradio that requires pydantic<2.

## Running

```bash
pip install -r requirements.txt
python app.py
```

The app will start on port 7860.
