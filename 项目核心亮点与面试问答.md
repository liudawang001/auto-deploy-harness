# 项目核心亮点与面试问答

## 一、核心亮点

### 1. 确定性控制 + 受控 Agent 推理

亮点说明：

- Python controller 负责状态机、命令执行、安全策略、下载、verify 和 report。
- LLM/Claude Code 只作为 advisor，不直接执行命令，不直接修改源码。
- 解决了大厂面试中最关心的 Agent 安全问题：LLM 不能越权。

面试表达：

> 我没有做一个“让 LLM 随便跑命令”的部署助手，而是把 Agent 放在受控位置。确定性模块负责执行边界，LLM 只做项目阅读、日志理解和修复建议。

### 2. Evidence-based Verify

亮点说明：

- 不把 HTTP 200 或端口开放当作成功。
- 每次 verify 生成唯一 trace_id。
- 支持 Gradio API discovery、Gradio queue follow-up、Streamlit DOM、Browser DOM、artifact sha256。

面试表达：

> verify 模块是我认为最有工程价值的部分。自动部署最怕假成功，所以我要求模型链路必须证明它处理了当前 trace，而不是只证明页面能打开。

### 3. 大模型资产管理

亮点说明：

- 自动识别 Hugging Face / ModelScope 模型引用。
- 生成 manifest。
- 使用 model_cache。
- 支持续传、并发下载、etag/sha256 校验。
- Git LFS pointer 检测。

面试表达：

> 开源模型部署的大头不是代码，而是权重。项目把模型资产作为一等公民处理，避免重复下载、错误缓存和 LFS pointer 假权重。

### 4. Python/CUDA/Torch 环境求解

亮点说明：

- 独立 `env_solve` 阶段。
- 根据本机 CUDA 选择 PyTorch wheel。
- 自动添加老 Gradio 兼容约束。
- 生成 GPU 包兼容矩阵。

面试表达：

> 我没有直接 pip install requirements，而是在安装前先求解依赖环境，把常见 CUDA/Torch/GPU 包冲突提前结构化。

### 5. Repair + Resume

亮点说明：

- failed/uncertain 会生成 repair plan。
- policy 检查修复动作。
- loop controller 限制同一问题重试次数。
- resume 从安全阶段重跑，不必全量重跑。
- report 中有 execution audit。

面试表达：

> 自动修复不能只看“能不能修”，还要看“允不允许修”和“从哪里安全重跑”。所以我做了 policy、attempt limit 和 safe rerun stage。

### 6. Skill + Memory + Promotion

亮点说明：

- skill 是阶段控制文档。
- memory 记录失败经验。
- promotion 把高频 memory 转成可审核 skill 更新。
- apply 前必须审批，并绑定 regression cases。

面试表达：

> 我把部署经验从一次性日志升级为可审计知识资产。memory 不是无限堆积，而是能 promotion 到 skill，但必须人工审批和回归验证。

### 7. Benchmark 回归体系

亮点说明：

- 35 个本地 benchmark case。
- 覆盖下载、缓存、Git LFS、verify false positive、repair、Docker、GPU 包矩阵、memory promotion。

面试表达：

> Agent 项目也要有回归体系。尤其是 verify 防误判和 repair policy，这类逻辑不能只靠人工试。

## 二、面试官可能追问与回答

### Q1：这个项目到底是不是 LangChain / Dify / Hermes？

回答：

不是。这个项目没有使用 LangChain、Dify 或 Hermes 作为主框架。它是自研 Python controller 架构。核心是 `TaskRunner` 编排阶段，LLM provider 和 Claude Code executor 只是可插拔组件。这样设计是为了更强的状态控制、安全边界和可测试性。

### Q2：为什么不用现成 Agent 框架？

回答：

自动部署涉及真实 shell、下载大模型、写缓存、启动服务和处理密钥。如果用通用 Agent 框架，很容易让 LLM 直接越权执行。这个项目的关键不是 tool calling，而是可审计执行控制，所以我选择自研轻量 controller，把 LLM 限制在 advisor/diagnose/repair proposal。

### Q3：verify 为什么不能只看 HTTP 200？

回答：

AI demo 常见假成功包括：首页 200 但模型没加载、Streamlit 错误页仍返回 200、Gradio API 形态变化、输出文件是旧文件、服务进程没退出但推理失败。所以 verify 必须用 trace_id 证明当前输入被处理，或用新产物 sha256 证明当前执行产生了结果。

### Q4：你负责 verify 模块，最难的点是什么？

回答：

最难的是避免 false positive。为了防止误判，我做了几层证据：

- HTTP response 必须包含当前 trace_id。
- Gradio queue 必须 follow-up event_id。
- artifact 必须是本次 trace 后新增/修改，且非空、可读、有 sha256。
- Browser DOM 必须包含 trace 或识别错误标记。
- 没有强证据就保持 uncertain，不硬判 passed。

### Q5：Gradio queue 是怎么处理的？

回答：

先读取 `/config`，选出 backend_fn 可用的 dependency。如果 queue 开启并有 api_name，就 POST `/call/<api_name>` 获取 event_id，然后 GET `/call/<api_name>/<event_id>` 读取结果。只有 follow-up 响应中包含当前 trace_id，才认为通过。

### Q6：如何处理大模型下载中断？

回答：

下载器使用 `.part` 文件和 HTTP Range 续传。每个文件下载后写 `.auto_harness_meta.json`，记录 size、sha256、etag。后续缓存命中时会校验 metadata，etag 变化会重新下载。

### Q7：Git LFS 为什么要单独做？

回答：

GitHub 仓库里经常只有 LFS pointer，不是真实权重。如果不检测，部署会以为模型文件存在，实际启动时才失败。项目解析 `.gitattributes` 和 pointer 文件，统计 size，缺 `git-lfs` 时直接进入 diagnosis，而不是继续假装权重就绪。

### Q8：env_solve 的价值是什么？

回答：

它把环境问题前置。比如老 Gradio 和 numpy 2/pydantic 2 冲突，Torch CPU/CUDA wheel 不匹配，flash-attn 需要 CUDA toolchain。与其盲装失败后再看日志，不如安装前输出约束、torch_solution 和 gpu_package_matrix。

### Q9：如果 LLM 给出危险修复怎么办？

回答：

repair plan 不会直接执行。所有 action 都要经过 `RepairPolicy`。需要安装、源码修改、secret、人工确认的动作都有独立权限判断。被拒绝也会写入 `repair_apply_result.json`，避免旧 overlay 被误复用。

### Q10：为什么要做 resume？

回答：

大模型部署成本高。下载和安装可能很久，如果 verify 失败后每次从 analyze 开始全量重跑，效率很低。项目支持从 safe stage 重跑，并复用前置阶段结果。report 中会记录 reused/rerun stages，方便审计。

### Q11：memory promotion 会不会污染 skill？

回答：

不会。默认只生成 proposal，不修改 skill。apply 前必须审批，并绑定 benchmark case。追加时用 marker 包裹，避免重复应用。内容不能包含密钥值、一次性路径或未经验证的 workaround。

### Q12：Docker backend 的安全边界是什么？

回答：

Docker backend 只是把命令包装成 `docker run` effective command。真实执行仍必须 `--execute`、`--allow-install/--allow-start`，且 `docker` 在命令白名单中。否则直接 command rejected。

### Q13：如何证明项目质量？

回答：

项目有单测和 benchmark 双层验证。当前 benchmark 覆盖下载续传、etag 缓存失效、Git LFS、Gradio queue、HTTP 200 false positive、artifact validation、repair loop、Docker backend、GPU package matrix、memory promotion 等关键路径。

### Q14：这个项目如果上线生产，还缺什么？

回答：

主要缺四类：

- 真实联网长耗时 E2E。
- 真实 Docker/GPU smoke。
- vLLM/OpenAI-compatible server verify。
- Dashboard 和更细粒度任务调度。

### Q15：你怎么总结自己的贡献？

回答：

我主要把自动部署 Agent 从“能跑脚本”提升成“可控、可验证、可恢复”的工程系统。尤其是 verify 模块，我用 trace evidence、artifact validation、Gradio queue follow-up 和 browser DOM 防止假成功；同时把失败结果接入 memory/repair/resume，形成闭环。

## 三、STAR 版项目经历

背景：

开源 AI 模型项目部署失败率高，失败原因包括依赖冲突、模型权重缺失、CUDA 不匹配、启动命令不准、验证误判。

任务：

设计一个自动部署 Agent，能从源码分析到环境部署、模型准备、服务启动、证据化验证、失败诊断和自动修复。

行动：

- 设计确定性 Python pipeline。
- 实现 verify 证据链。
- 增加模型下载缓存和 Git LFS 识别。
- 增加 env_solve CUDA/Torch 兼容求解。
- 增加 repair policy 和 resume。
- 增加 memory promotion 和 benchmark。

结果：

- 当前完成约 90%。
- 支持 Hugging Face、ModelScope、Git LFS、Gradio、Streamlit、Docker backend。
- 35 个 benchmark case 覆盖关键风险。
- 系统不会把 HTTP 200、端口开放或旧产物误判为成功。
